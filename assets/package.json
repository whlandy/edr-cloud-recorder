{
  "name": "web-record-project",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "playwright test --project=setup",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
